[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "korpusomat-docs"
authors = [{name = "IPI PAN", email = "lkobylinski@gmail.com"}]
dynamic = ["version", "description"]
